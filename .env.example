# Kopiera till .env lokalt och fyll i verkliga värden där.
# Commit aldrig .env med hemligheter.

APP_ENV=local
LOG_LEVEL=info

# Exempel, byt namn efter projektets behov:
# API_BASE_URL=https://example.local
# API_KEY=replace-with-local-value
# DATABASE_URL=replace-with-local-value
